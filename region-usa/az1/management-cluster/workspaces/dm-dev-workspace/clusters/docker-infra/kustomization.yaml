apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # CAPK Provider (Kubemark) namespace
  - capk-provider

  # CAPD Cluster base resources
  - bases

  # Kubemark hollow machines (attach to CAPD cluster for scale testing)
  - kubemark-hollow-machines
