apiVersion: apps.kommander.d2iq.io/v1alpha3
kind: AppDeployment
metadata:
  name: rook-ceph
  namespace: dm-dev-workspace
spec:
  appRef:
    kind: ClusterApp
    name: rook-ceph-1.18.6
  clusterConfigOverrides:
    - appVersion: 1.18.6
      clusterSelector:
        matchExpressions:
          - key: kommander.d2iq.io/workspace-name
            operator: In
            values:
              - dm-dev-workspace
      configMapName: ""

